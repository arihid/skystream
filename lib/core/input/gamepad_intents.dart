import 'package:flutter/widgets.dart';

/// Semantic intents for UI and Navigation
class AppSelectIntent extends Intent {
  const AppSelectIntent();
}

class AppBackIntent extends Intent {
  const AppBackIntent();
}

class AppContextIntent extends Intent {
  const AppContextIntent();
}
